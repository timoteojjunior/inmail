<h1>Listing messages</h1>

<table>
  

<% @messages.each do |message| %>
  <tr>
    <td width="200px" height="50px"><%= link_to message.title, message %></td>
    <td width="600px" height="50px"><%= message.body %></td>
    <td width="100px" height="50px"><%= message.created_at.strftime("%b, %d") %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Message', new_message_path %>
